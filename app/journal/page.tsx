// app/journal/page.tsx
// Route: /journal — the Mythic Journal: a narrative synthesis across a
// signed-in seeker's readings, not just a timeline of them.

import MythicJournal from '@/app/components/MythicJournal';

export default function JournalPage() {
  return <MythicJournal />;
}
