// app/letters/page.tsx
// Route: /letters — a signed-in seeker's kept Threshold Letters.

import ThresholdLetters from '@/app/components/ThresholdLetters';

export default function LettersPage() {
  return <ThresholdLetters />;
}
